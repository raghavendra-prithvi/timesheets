
<!--This is the form to share with friends via email-->

<h1> 
	<span class="red">5</span> Friends
</h1>	<br /><br />


	<div class="textwrap2" id="center">Help reach our goal by sharing with 5 friends and family. <br />
	Numbers <i>will</i> make a difference!	</div><br /><br />
<p class="textwrap2">



	<!--
	<% if signed_in? %>
			<% if @friendsleft.to_i == 5 %>
				You have <span class="red">5 shares</span> left!
			<% else %>
				<% if (@friendsleft.to_i < 1 || @friendsleft.to_i == 1) %>
					<% if @friendsleft.to_i == 1 %>
						Only <span class="red">1</span>  left!
					<% else %>
						Thank You for sharing! 
					<% end %>	
				<% else %>			
					You have <span class="red"><%= pluralize(@friendsleft, "friend") %></span> left! 
				<% end %>	
			<% end %>
	<% else %>
		You have <span class="red">5 friends</span> left!
	<% end %>
	--!>
	

</p>
	
	
	
	




<!--
<div class="row">
<div class="span6">
<%= form_tag((@user), :html => {:class => 'form-horizontal' }) do %>
	<% if signed_in? %>
		<h4><div id="left">From:   </div></h4>
		<%= text_field_tag :email, nil, :value => current_user.email, class: "span8" %>	
	<% else %>
		<h4><div id="left">From:   </div></h4>
		<%= text_field_tag :email, nil, :placeholder => "Your email here", class: "span8" %>
	<% end %>	
	<div id="left">To:   </div>
	<%= text_area_tag :to, nil, :rows => 5, class: "span12", :placeholder => "Separate multiple emails by commas, spaces, brackets. Alternatively, paste directly from your email"%>
	
	<%= text_area_tag :subject , nil, :rows => 1, class: "span12", :placeholder => "Subject" %>	

	<%= text_area_tag :message , nil, :rows => 5, class: "span12", :placeholder => "Message (don't forget to include a link!)"%> 
		
	<% if signed_in? %>
		<%= submit_tag "Send", class: "btn btn-large btn-primary"%>
	<% else %>
		<%= link_to "Please sign in to share!", signin_path, class: "btn btn-large btn-primary" %>
	<% end %>
	
<%end%>  
</div>


<div class="span6">
	<% if signed_in? %>
		<h4><div id="right1">Or...   </div></h4>
		<div class="textwrap2" id="center">Send a link to 5 friends from your personal email. Click the link below to send yourself a reminder!</div>
		<%= link_to "Send me a reminder email!", sendme_path, class: "btn btn-large btn-primary", id:"right" %>
	<% end %>
</div>
</div>
--!>



<div id="center">

	<% if signed_in? %>
		<%= link_to "Send me a reminder email!", sendme_path, class: "btn btn-large btn-primary" %>
	<% else %>
		<%= link_to "Please sign in to share!", signin_path, class: "btn btn-large btn-primary" %>
	<% end %>

</div>






